module.exports = {
  // Fonts are bundled through the `expo-font` config plugin (see app.json).
  // This entry keeps `npx react-native-asset` working for any non-Expo tooling.
  assets: ["./assets/fonts"],
}
